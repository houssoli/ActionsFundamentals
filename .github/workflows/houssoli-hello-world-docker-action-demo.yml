name: Test houssoli/hello-world-docker-action@v1
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: houssoli/hello-world-docker-action@v1
        with:
          who-to-greet: '@octocat'